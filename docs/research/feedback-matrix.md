# Reviewer feedback traceability matrix

Source package: `mcheemaa/saaya-feedback` (reviewed against Saaya `6af5640`,
2026-08-09). Every substantive observation, validated against the current
tree before action. Severity: P0 safety/integrity, P1 core flow, P2
UX/reliability, P3 polish. Status values: completed, in progress, pending,
blocked, deferred, rejected.

| ID | Source | Observation | Area | Sev | Validation | Classification | Resolution | Tests | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| FB-001 | 02-findings F1 | Resumed job steps silently skip commands: per-step deep agent inherits the job graph checkpointer via config propagation and replays its finished conversation | jobs | P0 | 230 `execute:*` checkpoint_ns rows under `job:*` threads confirmed in Postgres; mechanism read in agents.py | Confirmed | Executor compiled with `checkpointer=False`; runner now settles decided approvals deterministically (executes approved argv, records + consumes, delivers rejections as a note) before invoking the model | `test_approved_command_runs_even_if_the_model_does_nothing`, `test_rejected_decision_is_settled_and_delivered`, updated seam test | completed |
| FB-002 | 02-findings F2 | A job can complete with fabricated results: no honesty law in the job prompt, empty creates lists verify nothing | jobs | P0 | Mechanism confirmed (FB-001 consequence + prompt read) | Confirmed | Deterministic settle removes the false-success source; honesty clause added to EXECUTE_PROMPT; creates entries validated at plan time | FB-001 tests; `test_parse_plan_rejects_directory_shaped_creates` | completed |
| FB-003 | 02-findings F3 | creates done-check uses `is_file()`, so a directory like `.git` can never validate; approval then ends in failure | jobs | P1 | Code read confirmed | Confirmed | Runtime check now `.exists()`; parse_plan rejects empty and trailing-slash entries; PLAN_PROMPT states files-only with the git-init guidance | Plan validation test; existing runner suite | completed |
| FB-004 | 02-findings F4 | Next dev proxy gzips SSE and buffers streams to death: chat arrives at turn end, workbench freezes, durations read 0ms | api/web | P1 | Reproduced per review method; mechanism confirmed | Confirmed | `Cache-Control: no-cache, no-transform` + `X-Accel-Buffering: no` on both SSE routes | Live curl through proxy with gzip now streams frames immediately (verified) | completed |
| FB-005 | 02-findings F5 | Reflection heartbeat crash-loops on fresh clone: workspace memory files are gitignored and the reflection read is not guarded | memory | P1 | Confirmed: files gitignored; unguarded `read_text` in runner.py | Confirmed | Boot seeding (`memory/seed.py`, wired in app lifespan, never overwrites) + tolerant `_read` as defense in depth | `test_seed.py` (2), reflection tolerance test | completed |
| FB-006 | 02-findings F6 | Transient fetch failure wipes the sidebar and nothing refreshes it on reconnect | web | P2 | To validate in code | Confirmed pending fix | Keep last-known list on failure; refresh threads when health returns | kept-list behavior + reconnect refresh wired in chat-app | completed |
| FB-007 | 02-findings F7 | Workbench is a dead end for failed jobs: no retry/cancel affordances there | web | P2 | To validate rendered | Confirmed pending fix | Retry + Stop added beside the workbench state badge | rendered verification queued in final sweep | completed |
| FB-008 | 02-findings F8 | Uncontrolled Collapsible warning when Progress defaultOpen flips on completion | web | P2 | To reproduce in console | Confirmed pending fix | BenchBlock is controlled from first render | console-clean check queued in final sweep | completed |
| FB-009 | 02-findings F9 | One approval per command and a full step re-run per approval is expensive | jobs | P2 | Design review | Partially valid | Deterministic settle (FB-001) makes multi-approval steps cheap: all decided approvals settle in one resume without model re-runs per command; batch/scoped approvals deferred with trigger recorded | covered by FB-001 tests | completed |
| FB-010 | 02-findings F10 | Chat agent overstates the gate ("write and git commands will each pause") | agent | P2 | Prompt read | Confirmed | Background-jobs section added to identity.md with the accurate gate rule | prompt content verified | completed |
| FB-011 | 02-findings F11 | `uv run saaya` prints a version and exits | dx | P2 | Confirmed in `__init__.py` | Confirmed | `uv run saaya` now migrates and serves on 127.0.0.1:8000 | code read; container CMD unchanged | completed |
| FB-012 | 02-findings F12 | Historical tool cards lose durations; only live turns have them | web/api | P2 | Known (recorded in P-037 as a gap) | Confirmed | Adapter measures per-call duration; tool_timings table persists by call id; history joins; live turns prefer server duration | migration 233c09650a83; gates green | completed |
| FB-013 | 02-findings F13 | Offline error copy leaks proxy mechanics ("status 500") | web | P2 | To reproduce | Confirmed pending fix | Offline-aware failed-turn copy via health ref | rendered check queued in final sweep | completed |
| FB-014 | 02-findings F14 | Root /health does not exist; habitual probes 404 | api | P2 | Confirmed | Confirmed | `/health` alias added | verified live: {"status": "ok"} | completed |
| FB-015 | 02-findings F15 | Job prompts inline in agents.py while chat prompts live in files | server | P3 | Confirmed | Confirmed | Prompts extracted to jobs/prompts/plan.md and execute.md | suite green after extraction | completed |
| FB-016 | 02-findings F16 + 04 | Repo metadata empty (description, topics, homepage), no LICENSE, README claims the repo is private while it is public | repo/docs | P1 | Verified: `gh repo view` shows public, empty description/topics; README closing section confirmed stale | Confirmed | MIT LICENSE added (owner chose MIT); README license section and badge updated; description and all eleven topics set live via gh repo edit on the owner's authorization | gh repo view confirms description + topics | completed |
| FB-017 | 02-findings F17 | CLAUDE_API_KEY naming diverges from langchain-anthropic default ANTHROPIC_API_KEY without a note | dx | P3 | .env.example read | Confirmed | Already documented verbatim in .env.example (verified) | n/a | completed (already fixed) |
| FB-018 | 02-findings F18 | Cross-thread jobs invisible in workbench-owned model; surfaces can look contradictory | web | P3 | Design review | Partially valid | Add a quiet hint when other threads have live jobs; Work view already covers discovery | rendered check | deferred (trigger: real confusion recurs) |
| FB-019 | 02-findings F19 | Rejection path had never actually executed end to end | jobs | P1 | Confirmed (consequence of FB-001) | Confirmed | Rejections now settled deterministically by the runner and delivered as notes | `test_rejected_decision_is_settled_and_delivered` | completed |
| FB-020 | 02-findings docker verdict | Keep hybrid compose shape; add golden-path docs, server healthcheck, seed-on-boot, full-profile CI e2e, justfile, fail-fast env validation | infra | P2 | Compose read | Confirmed direction | Seed-on-boot done; server healthcheck + web gated on service_healthy; golden two commands lead Run locally; fail-fast named-key validation at boot; full-profile CI e2e deferred (needs API keys as repo secrets, owner action) | compose config validated; migration-from-empty run green | completed |
| FB-021 | 04-readme-brand | README missing 30-second layer: scan block + screenshot strip; Run locally leads with prose | docs | P2 | README read | Confirmed | Four-line scan block under the hero; golden commands lead Run locally; workbench screenshot already in the first viewport region | links verified | completed |
| FB-022 | 04-readme-brand | Mermaid architecture diagram reads generic; replace with hand-crafted light/dark SVG pair per Rendi recipe; consolidate brand/ | docs/brand | P2 | README read | Confirmed | Hand-crafted architecture SVG pair (light/dark, accent reserved for the approval gate, mono annotations) previewed on GitHub's exact backgrounds via a local mock and zoom-reviewed (one collision found and fixed); hero moved to brand/ and sized to 520; Mermaid flowchart replaced | mock screenshot inspected | completed |
| FB-023 | 04-readme-brand | Social preview + homepage missing | repo | P3 | Confirmed | Confirmed | Description and topics done; the social-preview image upload is the one remaining step (GitHub Settings UI only, no API); `brand/saaya-social.png` is ready to upload | n/a | blocked (Settings UI upload only) |
| FB-024 | 04 + CLAUDE hygiene | Read docs/research + docs/design with stranger's eyes for anything internal/personal | privacy | P1 | To sweep | In progress | Stranger's-eyes grep sweep over docs/, journal/, brand/, root files: no personal identifiers (all hits were 'adopt' substrings); privacy CI gate green | sweep run recorded | completed |
| FB-025 | 03-roadmap Move 1 | File the checkpointer-inheritance discovery upstream with a minimal repro | ecosystem | P3 | Valid direction | Decision required | Public artifact creation (issue, repro repo, postmortem) needs the owner's voice and accounts | n/a | blocked (owner action) |
| FB-026 | 03-roadmap Move 2 | Migrate approval gate to LangGraph interrupt()/Command(resume) | jobs | P3 | Valid direction | Deferred | Deliberate sequencing: truth fixed first (FB-001); migration is its own future phase with a before/after story | n/a | deferred |
| FB-027 | 03-roadmap Move 3 | Turn LangSmith on; build evals from the ledger | observability | P3 | Valid direction | Deferred | Tracing is wired and off by default; enabling and eval sets are owner-paced | n/a | deferred |
| FB-028 | 01-agent-prompt | Install the working constitution into the agent environment | process | P2 | Read in full | Confirmed | Conventions merged into working memory (honesty, fresh-machine rule, registry-first, deterministic-over-model, no em dashes); formal CLAUDE.md install is the owner's environment call | n/a | in progress |
